<%= form_with(model: project_access) do |form| %>
  <% if project_access.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(project_access.errors.count, "error") %> prohibited this project_access from being saved:</h2>

      <ul>
        <% project_access.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :project_id %>
    <%= form.text_field :project_id %>
  </div>

  <div class="field">
    <%= form.label :access %>
    <%= form.number_field :access %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
